threshold:
  type: object
  properties:
    mode:
      title: Threshold Mode
      type: string
      default: absolute
    steps:
      title: Threshold Steps
      type: array
      items:
        type: object
        properties:
          color:
            type: string
          value:
            type: integer
