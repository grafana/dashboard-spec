threshold:
  type: object
  properties:
    mode:
      type: string
      default: absolute
    steps:
      title: Threshold Steps
      type: array
      items:
        type: object
        properties:
          color:
            type: string
          value:
            type: integer
