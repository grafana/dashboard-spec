override:
  type: object
  properties:
    matcher:
      type: oject
      properties:
        id:
          type: string
        options:
          type: string
    properties:
      type: array
      items:
        type: object
        properties:
          id:
            type: string
          value:
            type: integer
